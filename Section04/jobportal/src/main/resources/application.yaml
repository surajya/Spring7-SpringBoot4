logging:
  pattern:
    console:  ${CONSOLE_LOG_PATTERN:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %cyan(%msg%n)}

spring:
  datasource:
    url: jdbc:h2:file:D:/JAVA/UDemy/Spring7-SpringBoot4/Section04/jobportal;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
  #    mode: always
      schema-locations:
      - classpath:sql/jobportal-schema.sql
      data-locations:
      - classpath:sql/jobportal-data.sql
